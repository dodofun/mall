import React, {useEffect} from 'react'
import {View} from '@tarojs/components'
import './index.scss'

export default function () {
  useEffect(() => {}, [])

  return <View className="footer">—— 我是有底线的 ——</View>
}
